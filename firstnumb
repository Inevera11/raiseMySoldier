def undivisable32(n):
    i = 1
    output = []
    while i <= n:
        if i%2 != 0 or i%3 !=0:
            output.append(i)
        i+=1
    return output
print(f"{' '.join([str(item) for item in undivisable32(13)])} dziękuję")
